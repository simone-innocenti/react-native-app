import * as React from "react";

function globalScreenOptions() {
  return {
    headerStyle: {
      backgroundColor: "#000259",
      height: 80,
    },
    headerTitleStyle: {
      color: "#FFF",
    },
    headerTintColor: "#FFF",
  };
}

export default globalScreenOptions;
